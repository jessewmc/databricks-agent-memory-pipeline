[project]
name = "agent-memory-pipeline-bundle"
version = "0.1.0"
description = "Python Databricks Asset Bundle for the pi <> Lakebase memory testbed"
requires-python = ">=3.11"
dependencies = [
    "databricks-bundles>=1.4.0",
]

[tool.uv]
package = false
